# Python Interview Questions

Put interview-style questions and sample answers here.


What is the difference between mutation and rebinding?

A strong answer is:

Mutation changes the state of an existing object while keeping the same object identity. Rebinding changes which object a variable refers to, leaving the original object unchanged.


A function is an object that contains executable code and metadata.

The closure preserves access to the referenced object.

variables hold references to those objects.


A decorator is simply:
A function that takes another function, adds behavior, and returns a new function.